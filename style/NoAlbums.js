import { StyleSheet } from "react-native";

const style = StyleSheet.create({
  noAlbums: { flex: 1, justifyContent: "center", paddingTop: 30 },
  text: { flex: 1, alignSelf: "center", fontSize: 18 },
});
export default style;
